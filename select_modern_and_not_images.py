import sys
import os
import random
import pandas as pd

if __name__ == '__main__':
    folder = sys.argv[1]
    i=0
    wikiart_info = pd.read_csv(
    while i!==2:
        
        image_name = random.choice(os.listdir(folder))
        if "real" in  image_nam
        print("The chosen modern image for this song is", image_name)
        
